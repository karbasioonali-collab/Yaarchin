# نمونه‌ی متغیرهای محیطی. مقدار واقعی را فقط در .env (محلی) یا پنل لیارا بگذارید؛ هرگز در ریپو.
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/yarchin
# کلید ۳۲ بایتی base64 برای رمز کردن رمز TOTP:  openssl rand -base64 32
APP_ENCRYPTION_KEY=
