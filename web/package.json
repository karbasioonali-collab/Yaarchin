{
  "name": "yarchin-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "typecheck": "next typegen && tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "node --env-file-if-exists=.env scripts/migrate.mjs",
    "db:check": "drizzle-kit check",
    "db:seed": "node --env-file-if-exists=.env scripts/seed.mjs",
    "create-admin": "node --env-file-if-exists=.env scripts/create-admin.mjs",
    "postbuild": "node scripts/verify-standalone.mjs"
  },
  "dependencies": {
    "@node-rs/argon2": "^2.2.1",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "drizzle-orm": "^0.45.3",
    "next": "16.3.6",
    "pg": "^8.23.0",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "server-only": "^0.0.1",
    "typescript": "^5"
  },
  "devDependencies": {
    "@types/pg": "^8.23.1",
    "drizzle-kit": "^0.31.11",
    "eslint": "^9",
    "eslint-config-next": "16.3.6"
  },
  "engines": {
    "node": ">=20.9"
  }
}
